# notes

Follows [`protocols/notes.md`](../protocols/notes.md), with one structural
difference worth stating before anything else.

## Module notes are `doc.ts`, beside the code

The protocol's default is `notes/modules/`, ephemeral, roughly one per file, and
most of the corpus. **This repository puts that kind in a `doc.ts` in the
package it describes** — `lib/kernel/doc.ts`, `components/forms/button/doc.ts` —
and keeps `notes/` for the transferable kinds only.

Two reasons, and the second is the load-bearing one:

- A module note goes stale when its file changes, so it should be in the diff
  that changes it. A note in a parallel tree is a note nobody opens on the day
  it stops being true.
- `doc.ts` is also the **oracle** for a package's spec tests. Splitting it into
  a contract half and a mechanics half is what makes sanitising a section cut —
  see [`techniques/contract-and-mechanics-halves`](techniques/contract-and-mechanics-halves.md).
  A module note that lived in `notes/` could not do that job.

So the lopsided ratio the protocol describes still holds; the disposable half
just lives in the code tree. What is here is what survives this project.

```
  notes/
    substrate/   dependency-scoped, version-stamped — carries to any project using that thing
    patterns/    this architecture
    techniques/  broadly true, rarely stale
    language/    a language's own behaviour
```

`concepts/` has no entries: this is a template with no domain. When a product is
built from it, that is where its vocabulary goes.

## Claim index

Generated by reading the first line of each note, not maintained by hand. The
point is that these are disagreeable sentences rather than labels — the ones you
do not recognise are the ones to open.

| | |
| --- | --- |
| [a-flex-child-will-not-shrink](language/a-flex-child-will-not-shrink.md) | A flex item's `min-width` computes to `auto`, not `0`, so an item refuses to narrow below its own content and overflows the row it is in. |
| [a-responsive-plot-can-shrink-its-meaning](techniques/a-responsive-plot-can-shrink-its-meaning.md) | A responsive SVG can preserve every data point while shrinking the labels that explain them below a readable size. |
| [a-layout-bound-is-not-a-viewport](techniques/a-layout-bound-is-not-a-viewport.md) | A layout's requested bounds do not guarantee that its finished picture fits the viewport, especially when labels and annotations are added after layout. |
| [css-var-with-no-fallback](language/css-var-with-no-fallback.md) | An undefined `var()` with no fallback does not fall through to the rest of the declaration — it invalidates the whole custom property. |
| [fieldset-does-not-shrink](language/fieldset-does-not-shrink.md) | `<fieldset>` carries a user-agent `min-inline-size: min-content` that no other element has, so one inside a flex or grid container will not shrink below its widest child and pushes the whole layout open. |
| [no-question-mark-operator](language/no-question-mark-operator.md) | Porting an errors-as-values design into TypeScript costs exactly one thing: there is no propagation operator, so dependent sequencing stays an explicit early return forever. |
| [a-backend-graduates-one-domain-at-a-time](patterns/a-backend-graduates-one-domain-at-a-time.md) | *Is there a backend* and *is this domain finished* are two questions, and a composition root that asks only the first makes the first endpoint to ship wait for the last. |
| [a-root-is-handed-its-context](patterns/a-root-is-handed-its-context.md) | The tier that knows every other tier does not have to be the one tier that cannot leave the framework — hand it the token, the plan and the base url, and only the call site stays runtime-specific. |
| [a-service-takes-the-client](patterns/a-service-takes-the-client.md) | The usual justification for handing a service its transport is testability; the reason that actually pays is that it makes the whole tier byte-identical in three frameworks. |
| [absence-is-a-value-not-a-failure](patterns/absence-is-a-value-not-a-failure.md) | A read whose emptiness is a legitimate answer should say so in its type, and the kind that means *not there* should then be impossible for that read to produce. |
| [cascade-layers-as-a-tier-model](patterns/cascade-layers-as-a-tier-model.md) | Declaring layer order once makes "a caller may override a primitive" a property of the cascade rather than a convention about specificity. |
| [failures-split-by-who-may-narrow](patterns/failures-split-by-who-may-narrow.md) | An error union should be cut in two — the kinds any call can produce, and the kinds that vary by operation — because only the second half is a caller's to declare away. |
| [fault-injection-is-a-decorator](patterns/fault-injection-is-a-decorator.md) | A transport that returns failures instead of throwing them makes breaking things a wrapper around the port — no patched global, no interceptor, and nothing above it can tell. |
| [navigation-derived-from-the-page](patterns/navigation-derived-from-the-page.md) | A navigation read from the rendered DOM cannot list something that is not there, which is a property a hand-kept list can only promise. |
| [portable-by-having-no-alias](patterns/portable-by-having-no-alias.md) | A tier is copyable between sibling projects exactly when it names no framework and no path alias — and the alias is the half people forget. |
| [retry-belongs-to-the-error](patterns/retry-belongs-to-the-error.md) | Whether to try again, and how long to wait, is knowledge the failure already has — a cache configured with `retry: 3` is guessing on behalf of a type that could have told it. |
| [the-browser-asks-its-own-server](patterns/the-browser-asks-its-own-server.md) | Putting the session in an HttpOnly cookie decides your whole client-side data topology, not just where a token lives — the page can no longer call the API at all, and every browser-side read has to go through a door you write. |
| [the-shell-owns-the-interaction](patterns/the-shell-owns-the-interaction.md) | A shell tier's most valuable member is the one nobody thinks to put there — the identity of the current user action, without which a correlation id is just a second request id. |
| [two-tier-colour-tokens](patterns/two-tier-colour-tokens.md) | A theme should swap which rung of a fixed ramp a role points at, never the ramp itself. |
| [value-inward-exception-at-the-edge](patterns/value-inward-exception-at-the-edge.md) | Errors as values and a framework that demands throws are not in conflict, as long as exactly one function converts and everybody knows which one. |
| [abortsignal-timeout-vs-abort](substrate/abortsignal-timeout-vs-abort.md) | `AbortSignal.timeout` raises a `TimeoutError`; aborting a controller yourself raises an `AbortError`, which is the same thing a caller's own cancellation raises — so a hand-rolled timeout is indistinguishable from a user pressing cancel. **True of the WHATWG fetch/AbortSignal behaviour as implemented in Node 24 and current browsers · verified 2026-09-09** |
| [jsdom-drops-layered-css](substrate/jsdom-drops-layered-css.md) | A test environment that applies a plain CSS rule and silently ignores the identical rule inside `@layer` will pass every style assertion you write against a design system where all the CSS is layered — because the class it is checking does nothing there either. **True of `jsdom` 30 under `vitest` 5 · measured 2026-09-09** |
| [next-redacts-server-errors](substrate/next-redacts-server-errors.md) | An error thrown in a server component is redacted before a client error boundary sees it in production — the message is replaced and only a digest survives — so anything the client must actually know has to travel as data. **True of Next 16.3.4, App Router, production builds · read, not measured here** |
| [radio-arrows-may-move-without-checking](substrate/radio-arrows-may-move-without-checking.md) | WORKING — a radio group's arrow keys are specified to move focus AND check, but the implementation here does it through a listener that runs after the focus has already moved, and in a headless DOM the option arrives focused and unchecked. **True of `radix-ui` 1.6.7 · measured under `jsdom` 30, NOT verified in a browser · 2026-09-09** |
| [radix-slot-merge](substrate/radix-slot-merge.md) | Slot's merge is asymmetric on purpose: child props win, `className` and `style` merge, and handlers compose child-first. **True of `radix-ui` 1.6.7 · verified 2026-09-08** |
| [screenshot-caret-hiding-mutates-the-dom](substrate/screenshot-caret-hiding-mutates-the-dom.md) | A screenshot can alter the DOM while hydration is still comparing it, so a warning observed during capture may originate in the measuring harness. |
| [turbopack-css-chunk-order](substrate/turbopack-css-chunk-order.md) | A `@layer` order statement can only order layers it has not already seen, so the file carrying it must be the first stylesheet in the document. **True of Next 16.3.4 with Turbopack · verified 2026-09-08** |
| [a-cloning-wrapper-demands-a-cooperating-child](techniques/a-cloning-wrapper-demands-a-cooperating-child.md) | A wrapper that works by adding props to its child is making a requirement of that child which its type does not express — and when the child ignores them, nothing fails except the thing the wrapper existed to guarantee. |
| [a-comment-is-a-missing-union](techniques/a-comment-is-a-missing-union.md) | Whether a shape needs a discriminated union is decided by whether its fields are correlated or independent — and the most reliable signal that they are correlated is that somebody wrote a comment saying so. |
| [a-component-reference-is-a-function](techniques/a-component-reference-is-a-function.md) | What may cross into a client component is decided by whether it serialises, and the values people forget are the ones that do not look like functions — an icon sitting in a configuration object is a function. |
| [a-fixture-should-behave-like-a-server](techniques/a-fixture-should-behave-like-a-server.md) | A fixture that answers instantly, always succeeds and carries no metadata is a fixture every screen is quietly built against — and the differences all surface on the day the real endpoint lands, which is the worst day to find them. |
| [a-portal-splits-the-two-trees](techniques/a-portal-splits-the-two-trees.md) | A portal moves an element's DOM parent and leaves its framework parent alone, so half of what "parent" means follows it and half stays behind — and which half is which surprises people in both directions. |
| [a-shell-preview-needs-its-own-document](techniques/a-shell-preview-needs-its-own-document.md) | A shell preview embedded in a gallery can borrow the very dependencies it is supposed to demonstrate owning. |
| [a-short-circuit-inherits-what-it-skipped](techniques/a-short-circuit-inherits-what-it-skipped.md) | Anything that answers instead of calling through takes on every obligation of the path it bypassed — and the ones it forgets are invisible, because what it returns still looks right. |
| [a-skeleton-is-a-shape-not-a-spinner](techniques/a-skeleton-is-a-shape-not-a-spinner.md) | A skeleton earns its complexity by being the shape of what is coming; anything else is a spinner with more code and a worse reveal. |
| [a-state-is-not-a-variant](techniques/a-state-is-not-a-variant.md) | A variant is chosen by the author and a state comes from the data — so a state belongs on the element where the platform can see it, never in a prop that only sets a class. |
| [a-top-level-side-effect-cannot-be-tree-shaken](techniques/a-top-level-side-effect-cannot-be-tree-shaken.md) | A module that does something when it is imported is retained whole, however little of it you use — which is how a server-only fixture ends up in a browser bundle with nothing failing and nothing saying so. |
| [an-append-only-list-cannot-be-counted](techniques/an-append-only-list-cannot-be-counted.md) | A log that grows at the head breaks three things people reach for by reflex — page numbers, a total, and facet counts computed from what is on screen — and each of them looks like a nicety right up to the moment it traps a reader. |
| [an-overlay-borrows-focus](techniques/an-overlay-borrows-focus.md) | An overlay's obligations are one obligation — it borrows focus and must give it back — and every part of it breaks silently, in a way the person who broke it cannot see. |
| [announcements-are-a-budget](techniques/announcements-are-a-budget.md) | An interface that announces more is not more accessible — noise gets tuned out, and the announcements that mattered go with it. |
| [as-and-aschild-answer-different-questions](techniques/as-and-aschild-answer-different-questions.md) | Which of the two a component should take is decided by who owns the CONTENT, not by which is more flexible — and a component whose content is its whole point cannot use the flexible one. |
| [audit-the-source-not-the-render](techniques/audit-the-source-not-the-render.md) | Contrast is checkable from the stylesheet at build time, and that is stronger than checking it in a browser, because a `var()` chain resolves exactly and no screenshot is involved. |
| [baseline-before-you-improve](techniques/baseline-before-you-improve.md) | Score the artefact you already have, and record it, before building the one you expect to be better — otherwise the comparison is available only as an opinion. |
| [choose-the-cascade-tier-by-failure-mode](techniques/choose-the-cascade-tier-by-failure-mode.md) | Where a style belongs in the cascade is decided by what happens when something beats it, not by which tier is tidiest — and for a handful of rules the right answer is the one a layer model exists to discourage. |
| [composition-outlasts-configuration](techniques/composition-outlasts-configuration.md) | A component configured by data is a bet that the data is the only thing that varies, and the bet is lost the first time one cell needs a link. |
| [contract-and-mechanics-halves](techniques/contract-and-mechanics-halves.md) | A specification that doubles as design rationale must be split by section, not by sentence, or sanitising it later is a judgement call at the worst moment to make one. |
| [hand-the-caller-the-wiring](techniques/hand-the-caller-the-wiring.md) | When a component owns identifiers that a different element has to carry, the only arrangement in which forgetting them is visible is handing them over as named props. |
| [index-by-the-thing-not-the-argument](techniques/index-by-the-thing-not-the-argument.md) | A reference index has to be organised by what is being referenced. Group it by the point you want to make and the things themselves stop being findable. |
| [inject-emptiness-not-only-failure](techniques/inject-emptiness-not-only-failure.md) | A fault injector that can only break things covers the half you would have found anyway — the branches that ship unlooked-at are *empty* and *loading*. |
| [lookalike-controls-are-different-promises](techniques/lookalike-controls-are-different-promises.md) | Choosing between a checkbox, a switch and a toggle is not a styling decision — each announces a different promise about what happens next, and the wrong one tells a reader something untrue. |
| [negative-controls-catch-the-harness](techniques/negative-controls-catch-the-harness.md) | A measuring harness needs one case that must fail and one that must pass, because the failure it is most likely to have produces a beautiful uniform result. |
| [offer-nothing-that-does-nothing](techniques/offer-nothing-that-does-nothing.md) | A control that cannot change anything should not be a control — offering it costs a reader a stop, a decision, and some of the trust that the next one will do something. |
| [probe-before-writing-a-weaker-assertion](techniques/probe-before-writing-a-weaker-assertion.md) | An assertion written because you assumed the thing was unobservable is worse than no test — it reports coverage it does not have, and the observability is usually right there. |
| [shadowing-a-platform-attribute](techniques/shadowing-a-platform-attribute.md) | When a design system's vocabulary collides with the platform's, shadow the platform name rather than inventing a second one — but only where the platform's meaning is one nobody wants, and say so where it happens. |
| [shorthand-without-a-cascade-layer](techniques/shorthand-without-a-cascade-layer.md) | The objection to a utility layer is that it ships a second scale and competes in the cascade — remove both and the same shorthand is a typed accessor to the tokens rather than a rival to them. |
| [stable-key-from-unstable-data](techniques/stable-key-from-unstable-data.md) | A value derived fresh on every render cannot be a change signal, and the fix is to derive a primitive key from it rather than to memoise the derivation. |
| [the-first-paint-happens-before-your-code](techniques/the-first-paint-happens-before-your-code.md) | Anything that has to be true of the first frame cannot be decided by the component that renders it, which forces a second copy of the logic into something that runs earlier — and that copy, not the script, is the real cost. |
| [three-states-die-at-the-render](techniques/three-states-die-at-the-render.md) | Every tier can keep a distinction intact and one ternary at the last call site destroys it — so the collapse has to be made unwriteable by a component rather than forbidden by a rule. |
| [traversal-that-activates](techniques/traversal-that-activates.md) | Arrow-key traversal that activates as it moves is the right default and the wrong one the moment activation costs something — four tabs walked past becomes four panels loaded. |
